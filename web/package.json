{
	"name": "maripanatokana-web",
	"private": true,
	"version": "1.0.0",
	"type": "module",
	"scripts": {
		"copy-assets": "node scripts/copy-shared-assets.js",
		"build": "npm run copy-assets && cd svelte && npm run build"
	}
}
